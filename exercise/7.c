#include<stdio.h>
#include<string.h>
int main()
{
    char str[20]={0},str1[20]={0};
    int i;
    printf("input a string:");
    gets(str);
    strcpy(str1,str);
    for(i=0;i<strlen(str);i++)
    if(a[i]>=48&&a[i]<=57)
    {
         
    }
}